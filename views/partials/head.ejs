<!DOCTYPE html>
<html lang="en">
<head>
    <%- include noscript.ejs %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css.css">
    <link rel="icon" href="/logo.png" type="image/x-icon">
    <script src="/scripts/smoothScroll.js" type="text/javascript"></script>
    <title>Список заведений города</title>
</head>
<body>
	<div class="window_info">
		<p id="window_text">
            <%= currentPlace %>
        </p>
	</div>
	<header>
		<form class="logo" action="/" method="GET"><button id="mainBtn"></button></form>

		<div class="table_row">
                <% try { if (user == (null || undefined)) {%>
                <div class="enter_n_register table_row active">
                    <p class="e_n_r_text active e_n_r_enterBtn" id="enterBtn" onclick="window.location.href = '/enter';">
                    Вход
                    </p>
                    <div class="enterPng not_active" id="enterBtn" style="background-image: url(enter.png);" onclick="window.location.href = '/enter';"></div>

                    <p class="e_n_r_text active" id="lyambda" style="cursor: default;">λ</p>

                    <p class="e_n_r_text active e_n_r_registrationBtn" id="registerBtn" onclick="window.location.href = '/registration';">
                    Регистрация
                    </p>
                    <div class="registrationPng not_active" id="registerBtn" style="background-image: url(registration.png);" onclick="window.location.href = '/registration';"></div>
                </div>
                <% } else { %>
				<div class="profile_pic" style="background-image: <% try { if (user.logo) { %>url(http://<%=localURL%>/profile-pictures/<%=user._id%>)<% } else { %>url('/profilePic.png')<% } } catch (e) { } %>;"></div>
                <% } } catch {}%>
		</div>
	</header>
	<main <%if (currentPlace == 'Список заведений') { %> style="justify-content: flex-start;" <% } %> >
        <%try { if (user.accountType >= 1) { %>
        <div class="profile_pic_menu">
            <a id="accountBtn">Аккаунт</a>
            <a id="userSchoolsBtn">Ваши заведения</a>
            <% if (user.accountType == 2) { %>
            <a id="adminBtn">Админ панель</a>
            <% } %>
            <form action="/logout?_method=DELETE" method="POST">
                <button id="enterBtn" type="submit">Выйти из аккаунта</button>
            </form>

        </div>
        <% } } catch {}%>